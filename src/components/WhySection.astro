<section class="why-section">
  <div class="container">
    <div class="section-heading">
      <span class="section-label">Por qué Rekval</span>

      <h2>Decisiones patrimoniales con claridad, método y visión de futuro.</h2>

      <p>
        Nuestro trabajo busca simplificar decisiones complejas, entregando una
        estructura clara para proteger, administrar y proyectar el patrimonio.
      </p>
    </div>

    <div class="cards-grid">
      <article class="feature-card">
        <div class="icon-placeholder"></div>
        <h3>Mirada integral</h3>
        <p>
          Integramos planificación, inversión, protección y sucesión en una misma
          estrategia patrimonial.
        </p>
      </article>

      <article class="feature-card">
        <div class="icon-placeholder"></div>
        <h3>Acompañamiento cercano</h3>
        <p>
          Trabajamos junto a cada cliente para entender sus objetivos, etapa familiar
          y necesidades financieras.
        </p>
      </article>

      <article class="feature-card">
        <div class="icon-placeholder"></div>
        <h3>Estrategia de largo plazo</h3>
        <p>
          Diseñamos hojas de ruta que permiten tomar decisiones con perspectiva,
          orden y continuidad.
        </p>
      </article>
    </div>
  </div>
</section>

<style>
  .why-section {
    background: #ffffff;
    color: #07111f;
    padding: 120px 0;
  }

  .container {
    width: min(1120px, 92%);
    margin: 0 auto;
  }

  .section-heading {
    max-width: 820px;
    margin-bottom: 64px;
  }

  .section-label {
    display: inline-block;
    margin-bottom: 18px;
    color: #1a6d9f;
    font-size: 13px;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  h2 {
    margin: 0;
    font-size: clamp(36px, 5vw, 60px);
    line-height: 1.02;
    letter-spacing: -0.05em;
  }

  .section-heading p {
    max-width: 700px;
    margin: 26px 0 0;
    color: rgba(7, 17, 31, 0.68);
    font-size: 18px;
    line-height: 1.75;
  }

  .cards-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }

  .feature-card {
    min-height: 360px;
    padding: 34px;
    border-radius: 30px;
    background: #f5f7fa;
    border: 1px solid rgba(7, 17, 31, 0.08);
    box-shadow: 0 24px 70px rgba(7, 17, 31, 0.08);
  }

  .icon-placeholder {
    width: 58px;
    height: 58px;
    border-radius: 18px;
    margin-bottom: 80px;
    background:
      linear-gradient(135deg, #d5dbe5 0%, #eef1f5 100%);
    border: 1px solid rgba(7, 17, 31, 0.08);
  }

  .feature-card h3 {
    margin: 0;
    font-size: 26px;
    line-height: 1.1;
    letter-spacing: -0.04em;
  }

  .feature-card p {
    margin: 18px 0 0;
    color: rgba(7, 17, 31, 0.68);
    font-size: 16px;
    line-height: 1.65;
  }

  @media (max-width: 940px) {
    .cards-grid {
      grid-template-columns: 1fr;
    }

    .why-section {
      padding: 86px 0;
    }

    .feature-card {
      min-height: auto;
    }

    .icon-placeholder {
      margin-bottom: 44px;
    }
  }
</style>