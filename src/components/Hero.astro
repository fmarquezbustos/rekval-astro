<section class="hero">
  <div class="container">
    <div class="hero-content">
      <span class="eyebrow">REKVAL</span>

      <h1>Construimos estrategias para ordenar, proteger y proyectar tu patrimonio.</h1>

      <p>
        REKVAL reúne soluciones especializadas de inversión, ahorro, protección
        patrimonial y activos inmobiliarios para personas, familias y empresas.
        Cada decisión financiera debe responder a una estrategia clara.
      </p>

      <div class="hero-actions">
        <a class="btn primary" href="#areas">Conoce nuestras áreas</a>
        <a class="btn secondary" href="#contacto">Conversemos</a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 88vh;
    display: flex;
    align-items: center;
    overflow: hidden;
    padding: 140px 0 80px;
    background-image:
      linear-gradient(
        90deg,
        rgba(7, 17, 31, 0.9) 0%,
        rgba(7, 17, 31, 0.78) 34%,
        rgba(7, 17, 31, 0.44) 62%,
        rgba(7, 17, 31, 0.14) 100%
      ),
      linear-gradient(
        180deg,
        rgba(7, 17, 31, 0.18) 0%,
        rgba(7, 17, 31, 0.34) 100%
      ),
      url("/images/hero-rekval.png");
    background-size: cover;
    background-position: center right;
    background-repeat: no-repeat;
  }

  .container {
    width: min(1120px, 92%);
    margin: 0 auto;
  }

  .hero-content {
    width: 100%;
    max-width: 700px;
  }

  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 22px;
    color: #8fd3ff;
    font-size: 14px;
    font-weight: 900;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .eyebrow::before {
    content: "";
    width: 42px;
    height: 1px;
    background: #8fd3ff;
  }

  h1 {
    max-width: 700px;
    margin: 0;
    color: #ffffff;
    font-size: clamp(42px, 5.2vw, 68px);
    line-height: 1;
    letter-spacing: -0.055em;
  }

  p {
    max-width: 590px;
    margin: 26px 0 0;
    color: rgba(255, 255, 255, 0.82);
    font-size: 20px;
    line-height: 1.65;
  }

  .hero-actions {
    display: flex;
    gap: 14px;
    margin-top: 36px;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 52px;
    padding: 0 26px;
    border-radius: 999px;
    font-weight: 850;
    text-decoration: none;
    transition:
      transform 0.22s ease,
      background 0.22s ease,
      border-color 0.22s ease;
  }

  .btn:hover {
    transform: translateY(-2px);
  }

  .primary {
    background: #0b7a75;
    color: #ffffff;
    box-shadow: 0 14px 34px rgba(11, 122, 117, 0.28);
  }

  .primary:hover {
    background: #0e8c86;
  }

  .secondary {
    border: 1px solid rgba(255, 255, 255, 0.28);
    background: rgba(255, 255, 255, 0.06);
    color: #ffffff;
  }

  .secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.42);
  }

  @media (max-width: 820px) {
    .hero {
      min-height: 680px;
      padding-top: 120px;
      background-image:
        linear-gradient(
          180deg,
          rgba(7, 17, 31, 0.92) 0%,
          rgba(7, 17, 31, 0.76) 48%,
          rgba(7, 17, 31, 0.92) 100%
        ),
        url("/images/hero-rekval.png");
      background-position: center;
    }

    h1 {
      font-size: clamp(36px, 10vw, 52px);
    }

    p {
      font-size: 18px;
    }

    .hero-actions {
      flex-direction: column;
      align-items: stretch;
    }

    .btn {
      width: 100%;
    }
  }
</style>
