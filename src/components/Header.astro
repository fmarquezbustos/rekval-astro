<header class="site-header">
  <div class="nav-container">
    <a class="logo" href="#" aria-label="REKVAL">
      <img src="/images/logo-positivo.png" alt="REKVAL" />
    </a>

    <nav class="menu">
      <a href="#">Inicio</a>
      <a href="#quienes-somos">Quiénes Somos</a>
      <a href="https://rekval.com/" target="_blank" rel="noreferrer">
        Rekval Inversiones <span aria-hidden="true">↗</span>
      </a>
      <a href="https://rekvalglobal.com/" target="_blank" rel="noreferrer">
        Rekval Global <span aria-hidden="true">↗</span>
      </a>
      <a href="#areas">Propiedades</a>
      <a href="#contacto">Contacto</a>
    </nav>

    <a class="contact-button" href="#contacto">Conversemos</a>
  </div>
</header>

<style>
  .site-header {
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 20;
    padding: 18px 0;
    background: rgba(7, 17, 31, 0.58);
    backdrop-filter: blur(22px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .nav-container {
    width: min(1120px, 92%);
    margin: 0 auto;
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 34px;
  }

  .logo {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
  }

  .logo img {
    width: 148px;
    height: auto;
  }

  .menu {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 26px;
  }

  .menu a {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    color: rgba(255, 255, 255, 0.74);
    text-decoration: none;
    font-size: 13px;
    font-weight: 750;
    white-space: nowrap;
    transition: color 0.2s ease;
  }

  .menu a::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -8px;
    width: 0;
    height: 1px;
    background: #8fd3ff;
    transition: width 0.22s ease;
  }

  .menu a:hover {
    color: #ffffff;
  }

  .menu a:hover::after {
    width: 100%;
  }

  .menu span {
    color: #8fd3ff;
    font-size: 11px;
    line-height: 1;
  }

  .contact-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 42px;
    padding: 0 18px;
    border-radius: 999px;
    background: rgba(11, 122, 117, 0.88);
    color: #ffffff;
    font-size: 13px;
    font-weight: 850;
    text-decoration: none;
    box-shadow: 0 12px 30px rgba(11, 122, 117, 0.22);
    transition:
      transform 0.22s ease,
      background 0.22s ease;
  }

  .contact-button:hover {
    transform: translateY(-2px);
    background: #0e8c86;
  }

  @media (max-width: 980px) {
    .nav-container {
      grid-template-columns: auto auto;
      justify-content: space-between;
    }

    .menu {
      display: none;
    }
  }

  @media (max-width: 520px) {
    .contact-button {
      min-height: 38px;
      padding: 0 14px;
      font-size: 12px;
    }

    .nav-container {
      grid-template-columns: auto auto;
      gap: 18px;
      justify-content: space-between;
    }

    .logo img {
      width: 124px;
    }
  }
</style>
