<header class="site-header">
  <div class="top-bar">
    <div class="top-bar-container">
      <a class="top-link top-link-active" href="https://rekval.com/" target="_blank" rel="noreferrer">
        <span class="brand-prefix">Rekval </span>Inversiones <span aria-hidden="true">↗</span>
      </a>

      <a class="top-link" href="https://rekvalglobal.com/" target="_blank" rel="noreferrer">
        <span class="brand-prefix">Rekval </span>Global <span aria-hidden="true">↗</span>
      </a>

      <a class="top-link" href="/#areas">
        <span class="brand-prefix">Rekval </span>Propiedades
      </a>
    </div>
  </div>

  <div class="main-header">
    <div class="nav-container">
      <a class="logo" href="/" aria-label="REKVAL">
        <img src="/images/logo-rekval-nuevo.png" alt="REKVAL" />
      </a>

      <nav class="menu">
        <a href="/">Inicio</a>
        <a href="/#quienes-somos">Quiénes Somos</a>
        <a href="/#areas">Nuestras áreas</a>
        <a href="/#metodologia">Metodología</a>
        <a href="/#diferenciadores">Diferenciadores</a>
        <a href="/#contacto">Contacto</a>
      </nav>

      <a class="contact-button" href="/#contacto">Conversemos</a>

      <details class="mobile-menu">
        <summary aria-label="Abrir menú de navegación">
          <span></span>
          <span></span>
          <span></span>
        </summary>

        <nav class="mobile-panel">
          <a href="/">Inicio</a>
          <a href="/#quienes-somos">Quiénes Somos</a>
          <a href="/#areas">Nuestras áreas</a>
          <a href="/#metodologia">Metodología</a>
          <a href="/#diferenciadores">Diferenciadores</a>
          <a href="/#contacto">Contacto</a>

          <div class="mobile-divider"></div>

          <a href="https://rekval.com/" target="_blank" rel="noreferrer">
            Rekval Inversiones <span aria-hidden="true">↗</span>
          </a>
          <a href="https://rekvalglobal.com/" target="_blank" rel="noreferrer">
            Rekval Global <span aria-hidden="true">↗</span>
          </a>
          <a href="/#areas">Rekval Propiedades</a>
        </nav>
      </details>
    </div>
  </div>
</header>

<style>
  .site-header {
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 20;
  }

  .top-bar {
    background: #0a233a;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .top-bar-container {
    width: min(1160px, 92%);
    margin: 0 auto;
    display: flex;
    align-items: stretch;
    justify-content: flex-end;
    gap: 0;
    min-height: 46px;
    overflow-x: auto;
    scrollbar-width: none;
  }

  .top-bar-container::-webkit-scrollbar {
    display: none;
  }

  .top-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 46px;
    padding: 0 22px;
    border-radius: 0;
    color: rgba(255, 255, 255, 0.86);
    text-decoration: none;
    font-size: 13px;
    font-weight: 850;
    letter-spacing: -0.01em;
    white-space: nowrap;
    background: transparent;
    border: 0;
    border-left: 1px solid rgba(255, 255, 255, 0.14);
    transition:
      background 0.22s ease,
      color 0.22s ease,
      opacity 0.22s ease;
  }

  .top-link:last-child {
    border-right: 1px solid rgba(255, 255, 255, 0.14);
  }

  .top-link-active,
  .top-link:hover {
    background: #164266;
    color: #ffffff;
  }

  .top-link span {
    margin-left: 5px;
    font-size: 11px;
    line-height: 1;
  }

  .top-link .brand-prefix {
    margin-left: 0;
    margin-right: 4px;
    font-size: inherit;
    line-height: inherit;
  }

  .main-header {
    padding: 14px 0;
    background: rgba(255, 255, 255, 0.96);
    backdrop-filter: blur(24px);
    -webkit-backdrop-filter: blur(24px);
    border-bottom: 1px solid rgba(10, 35, 58, 0.08);
    box-shadow: 0 18px 50px rgba(10, 35, 58, 0.08);
  }

  .nav-container {
    width: min(1160px, 92%);
    margin: 0 auto;
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: center;
    gap: 30px;
  }

  .logo {
    display: inline-flex;
    align-items: center;
    text-decoration: none;
  }

  .logo img {
    width: 205px;
    height: auto;
    display: block;
  }

  .menu {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 24px;
  }

  .menu a {
    position: relative;
    display: inline-flex;
    align-items: center;
    color: #0a233a;
    text-decoration: none;
    font-size: 13px;
    font-weight: 800;
    letter-spacing: -0.01em;
    white-space: nowrap;
    transition:
      color 0.22s ease,
      opacity 0.22s ease;
  }

  .menu a::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -9px;
    width: 0;
    height: 2px;
    border-radius: 999px;
    background: #0b7a75;
    transition: width 0.22s ease;
  }

  .menu a:hover {
    color: #0b7a75;
  }

  .menu a:hover::after {
    width: 100%;
  }

  .contact-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 42px;
    padding: 0 20px;
    border-radius: 999px;
    background: #0b7a75;
    color: #ffffff;
    font-size: 13px;
    font-weight: 850;
    text-decoration: none;
    box-shadow: 0 12px 28px rgba(11, 122, 117, 0.24);
    transition:
      transform 0.22s ease,
      background 0.22s ease,
      box-shadow 0.22s ease;
  }

  .contact-button:hover {
    transform: translateY(-2px);
    background: #0e8c86;
    box-shadow: 0 16px 34px rgba(11, 122, 117, 0.3);
  }

  .mobile-menu {
    display: none;
    position: relative;
  }

  .mobile-menu summary {
    display: inline-flex;
    width: 42px;
    height: 42px;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 5px;
    border-radius: 999px;
    background: rgba(10, 35, 58, 0.05);
    border: 1px solid rgba(10, 35, 58, 0.12);
    cursor: pointer;
    list-style: none;
  }

  .mobile-menu summary::-webkit-details-marker {
    display: none;
  }

  .mobile-menu summary span {
    width: 17px;
    height: 1.5px;
    border-radius: 999px;
    background: #0a233a;
    transition:
      transform 0.2s ease,
      opacity 0.2s ease;
  }

  .mobile-menu[open] summary span:nth-child(1) {
    transform: translateY(6.5px) rotate(45deg);
  }

  .mobile-menu[open] summary span:nth-child(2) {
    opacity: 0;
  }

  .mobile-menu[open] summary span:nth-child(3) {
    transform: translateY(-6.5px) rotate(-45deg);
  }

  .mobile-panel {
    position: absolute;
    top: calc(100% + 14px);
    right: 0;
    width: min(320px, 88vw);
    display: grid;
    gap: 4px;
    padding: 14px;
    border-radius: 22px;
    background: rgba(255, 255, 255, 0.98);
    border: 1px solid rgba(10, 35, 58, 0.1);
    box-shadow: 0 28px 80px rgba(10, 35, 58, 0.18);
    backdrop-filter: blur(22px);
    -webkit-backdrop-filter: blur(22px);
  }

  .mobile-panel a {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    min-height: 46px;
    padding: 0 14px;
    border-radius: 14px;
    color: #0a233a;
    font-size: 14px;
    font-weight: 800;
    text-decoration: none;
  }

  .mobile-panel a:hover {
    background: rgba(11, 122, 117, 0.08);
    color: #0b7a75;
  }

  .mobile-panel span {
    color: #0b7a75;
    font-size: 12px;
  }

  .mobile-divider {
    height: 1px;
    margin: 8px 4px;
    background: rgba(10, 35, 58, 0.1);
  }

  @media (max-width: 1080px) {
    .menu {
      gap: 18px;
    }

    .menu a {
      font-size: 12px;
    }

    .logo img {
      width: 185px;
    }
  }

  @media (max-width: 980px) {
    .nav-container {
      grid-template-columns: auto auto auto;
      justify-content: space-between;
    }

    .menu {
      display: none;
    }

    .mobile-menu {
      display: block;
    }
  }

  @media (max-width: 640px) {
    .top-bar-container {
      width: 100%;
      justify-content: flex-start;
      padding: 0;
      overflow-x: auto;
    }

    .top-link {
      flex: 1 0 auto;
      min-height: 42px;
      padding: 0 14px;
      font-size: 12px;
    }
  }

  @media (max-width: 380px) {
    .top-link {
      padding: 0 12px;
      font-size: 11px;
    }

    .top-link .brand-prefix {
      display: none;
    }
  }

  @media (max-width: 520px) {
    .main-header {
      padding: 12px 0;
    }

    .contact-button {
      min-height: 38px;
      padding: 0 14px;
      font-size: 12px;
    }

    .nav-container {
      grid-template-columns: auto auto auto;
      gap: 14px;
      width: min(100% - 28px, 1120px);
    }

    .logo img {
      width: 145px;
    }
  }
</style>
