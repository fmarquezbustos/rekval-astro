<header class="site-header">
  <div class="nav-container">
    <a class="logo" href="#">REKVAL</a>

    <nav class="menu">
      <a href="#">Inicio</a>
      <a href="#quienes-somos">Quiénes Somos</a>
      <a href="#areas">Rekval Inversiones</a>
      <a href="#areas">Rekval Global</a>
      <a href="#areas">Rekval Propiedades</a>
      <a href="#contacto">Contacto</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    position: fixed;
    top: 0;
    width: 100%;
    z-index: 20;
    padding: 24px 0;
    background: rgba(7, 17, 31, 0.78);
    backdrop-filter: blur(18px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .nav-container {
    width: min(1120px, 92%);
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 28px;
  }

  .logo {
    color: #ffffff;
    font-size: 22px;
    font-weight: 900;
    letter-spacing: 0.14em;
    text-decoration: none;
  }

  .menu {
    display: flex;
    align-items: center;
    gap: 22px;
  }

  .menu a {
    color: rgba(255, 255, 255, 0.78);
    text-decoration: none;
    font-size: 13px;
    font-weight: 700;
    white-space: nowrap;
    transition: color 0.2s ease;
  }

  .menu a:hover {
    color: #ffffff;
  }

  @media (max-width: 1080px) {
    .menu {
      gap: 16px;
    }

    .menu a {
      font-size: 12px;
    }
  }

  @media (max-width: 920px) {
    .menu {
      display: none;
    }
  }
</style>