<section id="contacto" class="final-cta">
  <div class="container">
    <div class="cta-card">
      <div class="cta-content">
        <span class="section-label">Conversemos</span>

        <h2>Cada patrimonio tiene una historia, una realidad y un objetivo diferente.</h2>

        <p>
          Cuéntanos qué necesitas y te orientaremos hacia el área adecuada dentro
          del ecosistema REKVAL.
        </p>

        <div class="cta-actions">
          <a class="btn primary" href="mailto:contacto@rekvalglobal.com">
            Solicitar orientación inicial
          </a>
          <a class="btn secondary" href="#areas">
            Ver áreas de especialización
          </a>
        </div>
      </div>

      <div class="cta-form">
        <div class="form-card">
          <h3>Orientación inicial</h3>

          <div class="fake-form">
            <div class="fake-field">Nombre</div>
            <div class="fake-field">Correo</div>
            <div class="fake-field">Teléfono</div>
            <div class="fake-field">Tipo de consulta</div>
            <div class="fake-field large">Mensaje</div>
            <div class="fake-button">Enviar solicitud</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .final-cta {
    background: #f3f5f4;
    color: #102a43;
    padding: 120px 0;
  }

  .container {
    width: min(1120px, 92%);
    margin: 0 auto;
  }

  .cta-card {
    overflow: hidden;
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    align-items: stretch;
    min-height: 620px;
    border-radius: 40px;
    background:
      radial-gradient(circle at 88% 12%, rgba(11, 122, 117, 0.28), transparent 34%),
      linear-gradient(135deg, #07111f 0%, #102a43 52%, #0b2f44 100%);
    color: #ffffff;
    box-shadow: 0 34px 100px rgba(16, 42, 67, 0.18);
  }

  .cta-content {
    padding: 72px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .section-label {
    display: inline-block;
    margin-bottom: 20px;
    color: #8fd3ff;
    font-size: 13px;
    font-weight: 900;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  h2 {
    max-width: 720px;
    margin: 0;
    font-size: clamp(38px, 5vw, 64px);
    line-height: 0.98;
    letter-spacing: -0.06em;
  }

  p {
    max-width: 640px;
    margin: 28px 0 0;
    color: rgba(255, 255, 255, 0.74);
    font-size: 18px;
    line-height: 1.75;
  }

  .cta-actions {
    display: flex;
    gap: 14px;
    margin-top: 38px;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 50px;
    padding: 0 24px;
    border-radius: 999px;
    font-weight: 800;
    text-decoration: none;
  }

  .primary {
    background: #ffffff;
    color: #102a43;
  }

  .secondary {
    border: 1px solid rgba(255, 255, 255, 0.26);
    color: #ffffff;
    background: rgba(255, 255, 255, 0.06);
  }

  .cta-form {
    display: flex;
    align-items: center;
    padding: 34px;
  }

  .form-card {
    width: 100%;
    padding: 34px;
    border-radius: 32px;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.16);
    backdrop-filter: blur(18px);
    box-shadow: 0 24px 70px rgba(0, 0, 0, 0.22);
  }

  .form-card h3 {
    margin: 0 0 26px;
    font-size: 30px;
    line-height: 1.05;
    letter-spacing: -0.05em;
  }

  .fake-form {
    display: grid;
    gap: 14px;
  }

  .fake-field {
    min-height: 54px;
    padding: 0 18px;
    border-radius: 16px;
    display: flex;
    align-items: center;
    color: rgba(255, 255, 255, 0.48);
    font-size: 14px;
    font-weight: 700;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
  }

  .fake-field.large {
    min-height: 110px;
    align-items: flex-start;
    padding-top: 18px;
  }

  .fake-button {
    min-height: 54px;
    border-radius: 999px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #0b7a75;
    color: #ffffff;
    font-size: 15px;
    font-weight: 900;
  }

  @media (max-width: 940px) {
    .final-cta {
      padding: 86px 0;
    }

    .cta-card {
      grid-template-columns: 1fr;
      min-height: auto;
    }

    .cta-content {
      padding: 48px 32px;
    }

    .cta-form {
      padding: 0 32px 32px;
    }
  }

  @media (max-width: 620px) {
    .cta-actions {
      flex-direction: column;
      align-items: stretch;
    }

    .btn {
      width: 100%;
    }

    .form-card {
      padding: 26px;
    }
  }
</style>