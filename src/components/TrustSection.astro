<section class="process-section">
  <div class="container">
    <div class="section-heading">
      <span class="section-label">Nuestra forma de trabajar</span>

      <h2>Una metodología común para orientar cada decisión patrimonial.</h2>

      <p>
        Para diferenciar a REKVAL de una plataforma de distribución de productos,
        trabajamos con un proceso claro: comprender, ordenar, implementar y
        acompañar.
      </p>
    </div>

    <div class="process-grid">
      <article class="process-card">
        <span class="step-number">01</span>
        <h3>Comprender</h3>
        <p>
          Conocemos tu realidad, tus objetivos y las decisiones que quieres tomar.
        </p>
      </article>

      <article class="process-card">
        <span class="step-number">02</span>
        <h3>Ordenar</h3>
        <p>
          Priorizamos necesidades y construimos una estrategia coherente con tu
          etapa de vida.
        </p>
      </article>

      <article class="process-card">
        <span class="step-number">03</span>
        <h3>Implementar</h3>
        <p>
          Identificamos las soluciones adecuadas mediante la unidad especializada
          correspondiente.
        </p>
      </article>

      <article class="process-card">
        <span class="step-number">04</span>
        <h3>Acompañar</h3>
        <p>
          Realizamos seguimiento y ajustamos la estrategia cuando cambian tus
          objetivos o el entorno.
        </p>
      </article>
    </div>

    <div class="process-message">
      <div class="message-line"></div>

      <div>
        <h3>Una estrategia debe ser integral, pero también comprensible.</h3>

        <p>
          Trabajamos para que cada persona pueda entender las decisiones que está
          tomando y el propósito que cumple cada solución dentro de su planificación
          patrimonial.
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .process-section {
    position: relative;
    overflow: hidden;
    background:
      radial-gradient(circle at 82% 20%, rgba(11, 122, 117, 0.24), transparent 34%),
      linear-gradient(135deg, #07111f 0%, #102a43 52%, #0b2f44 100%);
    color: #ffffff;
    padding: 120px 0;
  }

  .process-section::before {
    content: "";
    position: absolute;
    right: -160px;
    bottom: -180px;
    width: 520px;
    height: 520px;
    border-radius: 999px;
    background: rgba(11, 122, 117, 0.18);
    filter: blur(24px);
  }

  .container {
    position: relative;
    z-index: 2;
    width: min(1120px, 92%);
    margin: 0 auto;
  }

  .section-heading {
    max-width: 880px;
    margin-bottom: 64px;
  }

  .section-label {
    display: inline-block;
    margin-bottom: 18px;
    color: #8fd3ff;
    font-size: 13px;
    font-weight: 900;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  h2 {
    margin: 0;
    max-width: 860px;
    font-size: clamp(36px, 5vw, 60px);
    line-height: 1.02;
    letter-spacing: -0.05em;
  }

  .section-heading p {
    max-width: 760px;
    margin: 26px 0 0;
    color: rgba(255, 255, 255, 0.74);
    font-size: 18px;
    line-height: 1.75;
  }

  .process-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 18px;
  }

  .process-card {
    min-height: 330px;
    padding: 30px;
    border-radius: 30px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.13);
    backdrop-filter: blur(16px);
    box-shadow: 0 28px 80px rgba(0, 0, 0, 0.18);
  }

  .step-number {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 58px;
    height: 58px;
    margin-bottom: 86px;
    border-radius: 18px;
    background: rgba(255, 255, 255, 0.1);
    color: #8fd3ff;
    font-size: 13px;
    font-weight: 900;
    letter-spacing: 0.12em;
    border: 1px solid rgba(255, 255, 255, 0.14);
  }

  .process-card h3 {
    margin: 0;
    font-size: 26px;
    line-height: 1.1;
    letter-spacing: -0.04em;
  }

  .process-card p {
    margin: 18px 0 0;
    color: rgba(255, 255, 255, 0.72);
    font-size: 16px;
    line-height: 1.65;
  }

  .process-message {
    display: grid;
    grid-template-columns: 120px 1fr;
    gap: 34px;
    align-items: start;
    margin-top: 72px;
    padding: 46px;
    border-radius: 34px;
    background: rgba(255, 255, 255, 0.07);
    border: 1px solid rgba(255, 255, 255, 0.12);
  }

  .message-line {
    width: 100%;
    height: 2px;
    margin-top: 18px;
    background: linear-gradient(90deg, #8fd3ff, rgba(143, 211, 255, 0));
  }

  .process-message h3 {
    max-width: 760px;
    margin: 0;
    font-size: clamp(28px, 4vw, 44px);
    line-height: 1.05;
    letter-spacing: -0.05em;
  }

  .process-message p {
    max-width: 760px;
    margin: 22px 0 0;
    color: rgba(255, 255, 255, 0.72);
    font-size: 17px;
    line-height: 1.7;
  }

  @media (max-width: 1040px) {
    .process-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 760px) {
    .process-section {
      padding: 86px 0;
    }

    .process-grid {
      grid-template-columns: 1fr;
    }

    .process-card {
      min-height: auto;
    }

    .step-number {
      margin-bottom: 44px;
    }

    .process-message {
      grid-template-columns: 1fr;
      padding: 32px;
      margin-top: 44px;
    }

    .message-line {
      width: 90px;
    }
  }
</style>