<section class="trust-section">
  <div class="container trust-grid">
    <div class="trust-content">
      <span class="section-label">Confianza y método</span>

      <h2>Un proceso claro para tomar mejores decisiones patrimoniales.</h2>

      <p>
        Sabemos que administrar un patrimonio implica decisiones relevantes:
        inversión, protección, planificación familiar, sucesión y continuidad.
        Por eso trabajamos con una metodología ordenada, transparente y adaptada
        a cada realidad.
      </p>

      <div class="stats-grid">
        <div class="stat-card">
          <strong>01</strong>
          <span>Diagnóstico patrimonial</span>
        </div>

        <div class="stat-card">
          <strong>02</strong>
          <span>Estrategia personalizada</span>
        </div>

        <div class="stat-card">
          <strong>03</strong>
          <span>Acompañamiento continuo</span>
        </div>
      </div>
    </div>

    <div class="trust-panel">
      <div class="panel-header">
        <div class="circle"></div>
        <div>
          <h3>Modelo 360°</h3>
          <p>Planificación, inversión, protección y sucesión.</p>
        </div>
      </div>

      <div class="panel-list">
        <div class="panel-item">
          <span></span>
          <p>Ordenamos la información clave del patrimonio.</p>
        </div>

        <div class="panel-item">
          <span></span>
          <p>Definimos prioridades y objetivos de largo plazo.</p>
        </div>

        <div class="panel-item">
          <span></span>
          <p>Construimos una hoja de ruta clara para avanzar.</p>
        </div>

        <div class="panel-item">
          <span></span>
          <p>Revisamos y ajustamos la estrategia en el tiempo.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .trust-section {
    position: relative;
    overflow: hidden;
    background:
      radial-gradient(circle at 82% 20%, rgba(87, 148, 185, 0.28), transparent 34%),
      linear-gradient(135deg, #07111f 0%, #0b1d30 48%, #10293d 100%);
    color: #ffffff;
    padding: 120px 0;
  }

  .trust-section::before {
    content: "";
    position: absolute;
    inset: auto -10% -35% auto;
    width: 520px;
    height: 520px;
    border-radius: 999px;
    background: rgba(143, 211, 255, 0.1);
    filter: blur(20px);
  }

  .container {
    width: min(1120px, 92%);
    margin: 0 auto;
  }

  .trust-grid {
    position: relative;
    display: grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap: 72px;
    align-items: center;
  }

  .section-label {
    display: inline-block;
    margin-bottom: 18px;
    color: #8fd3ff;
    font-size: 13px;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  h2 {
    max-width: 760px;
    margin: 0;
    font-size: clamp(36px, 5vw, 60px);
    line-height: 1.02;
    letter-spacing: -0.05em;
  }

  .trust-content p {
    max-width: 660px;
    margin: 28px 0 0;
    color: rgba(255, 255, 255, 0.76);
    font-size: 18px;
    line-height: 1.75;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 14px;
    margin-top: 42px;
  }

  .stat-card {
    padding: 22px;
    border-radius: 22px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
  }

  .stat-card strong {
    display: block;
    margin-bottom: 18px;
    color: #8fd3ff;
    font-size: 14px;
    letter-spacing: 0.12em;
  }

  .stat-card span {
    display: block;
    color: #ffffff;
    font-size: 15px;
    font-weight: 700;
    line-height: 1.35;
  }

  .trust-panel {
    padding: 36px;
    border-radius: 34px;
    background: rgba(255, 255, 255, 0.09);
    border: 1px solid rgba(255, 255, 255, 0.16);
    box-shadow: 0 34px 90px rgba(0, 0, 0, 0.3);
    backdrop-filter: blur(18px);
  }

  .panel-header {
    display: flex;
    gap: 18px;
    align-items: center;
    padding-bottom: 30px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.12);
  }

  .circle {
    width: 58px;
    height: 58px;
    flex: 0 0 auto;
    border-radius: 999px;
    background:
      radial-gradient(circle at 35% 35%, #ffffff 0%, #8fd3ff 36%, #1a6d9f 100%);
    box-shadow: 0 12px 40px rgba(143, 211, 255, 0.22);
  }

  .panel-header h3 {
    margin: 0;
    font-size: 28px;
    letter-spacing: -0.04em;
  }

  .panel-header p {
    margin: 8px 0 0;
    color: rgba(255, 255, 255, 0.68);
    line-height: 1.5;
  }

  .panel-list {
    display: grid;
    gap: 20px;
    margin-top: 30px;
  }

  .panel-item {
    display: grid;
    grid-template-columns: 14px 1fr;
    gap: 16px;
    align-items: start;
  }

  .panel-item span {
    width: 10px;
    height: 10px;
    margin-top: 8px;
    border-radius: 999px;
    background: #8fd3ff;
  }

  .panel-item p {
    margin: 0;
    color: rgba(255, 255, 255, 0.8);
    font-size: 17px;
    line-height: 1.6;
  }

  @media (max-width: 940px) {
    .trust-grid {
      grid-template-columns: 1fr;
      gap: 44px;
    }

    .trust-section {
      padding: 86px 0;
    }

    .stats-grid {
      grid-template-columns: 1fr;
    }
  }
</style>