<section class="intro-section">
  <div class="container intro-grid">
    <div class="intro-copy">
      <span class="section-label">Rekval Global</span>

      <h2>Una mirada integral para ordenar, proteger y proyectar tu patrimonio.</h2>

      <p>
        Acompañamos a personas, familias y empresas en la toma de decisiones
        patrimoniales con una visión estratégica, independiente y de largo plazo.
      </p>

      <p>
        Nuestro enfoque combina planificación, inversión, protección y sucesión,
        permitiendo construir una hoja de ruta clara para cada etapa financiera y
        familiar.
      </p>
    </div>

    <div class="intro-visual">
      <div class="image-placeholder">
        <span>Imagen / equipo / oficina</span>
      </div>
    </div>
  </div>
</section>

<style>
  .intro-section {
    background: #f5f7fa;
    color: #07111f;
    padding: 120px 0;
  }

  .container {
    width: min(1120px, 92%);
    margin: 0 auto;
  }

  .intro-grid {
    display: grid;
    grid-template-columns: 1fr 0.9fr;
    align-items: center;
    gap: 72px;
  }

  .section-label {
    display: inline-block;
    margin-bottom: 18px;
    color: #1a6d9f;
    font-size: 13px;
    font-weight: 800;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  h2 {
    margin: 0;
    font-size: clamp(36px, 5vw, 58px);
    line-height: 1.02;
    letter-spacing: -0.05em;
    max-width: 760px;
  }

  p {
    max-width: 620px;
    margin: 26px 0 0;
    color: rgba(7, 17, 31, 0.72);
    font-size: 18px;
    line-height: 1.75;
  }

  .intro-visual {
    display: flex;
    justify-content: center;
  }

  .image-placeholder {
    width: 100%;
    min-height: 480px;
    border-radius: 34px;
    background:
      linear-gradient(135deg, #d9dee7 0%, #eef1f5 100%);
    border: 1px solid rgba(7, 17, 31, 0.08);
    box-shadow: 0 30px 80px rgba(7, 17, 31, 0.12);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .image-placeholder span {
    color: rgba(7, 17, 31, 0.38);
    font-size: 15px;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  @media (max-width: 900px) {
    .intro-grid {
      grid-template-columns: 1fr;
      gap: 44px;
    }

    .intro-section {
      padding: 86px 0;
    }

    .image-placeholder {
      min-height: 340px;
    }
  }
</style>