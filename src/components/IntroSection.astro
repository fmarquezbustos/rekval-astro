<section id="quienes-somos" class="intro-section">
  <div class="container intro-grid">
    <div class="intro-copy">
      <span class="section-label">Quiénes somos</span>

      <h2>Una visión integral para cada etapa de tu patrimonio.</h2>

      <p>
        En REKVAL entendemos que construir patrimonio no significa solamente
        elegir productos financieros.
      </p>

      <p>
        Significa comprender tu realidad, definir prioridades y tomar decisiones
        coherentes con tus objetivos personales, familiares o empresariales.
      </p>

      <p>
        Nuestra marca reúne distintas áreas de especialización para acompañarte
        según tus necesidades y el momento en que te encuentres.
      </p>
    </div>

    <div class="intro-visual">
      <div class="image-placeholder">
        <img
          src="/images/quienes-somos.png"
          alt="Asesoría patrimonial en una reunión con clientes"
        />

        <div class="visual-content">
          <span>REKVAL</span>
          <strong>Una marca. Distintas áreas de especialización.</strong>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .intro-section {
    background: #f3f5f4;
    color: #102a43;
    padding: 120px 0;
  }

  .container {
    width: min(1120px, 92%);
    margin: 0 auto;
  }

  .intro-grid {
    display: grid;
    grid-template-columns: 1fr 0.9fr;
    align-items: center;
    gap: 72px;
  }

  .section-label {
    display: inline-block;
    margin-bottom: 18px;
    color: #0b7a75;
    font-size: 13px;
    font-weight: 900;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  h2 {
    margin: 0;
    max-width: 760px;
    font-size: clamp(36px, 5vw, 58px);
    line-height: 1.02;
    letter-spacing: -0.05em;
  }

  p {
    max-width: 620px;
    margin: 24px 0 0;
    color: rgba(16, 42, 67, 0.72);
    font-size: 18px;
    line-height: 1.75;
  }

  .intro-visual {
    display: flex;
    justify-content: center;
  }

  .image-placeholder {
    position: relative;
    width: 100%;
    min-height: 500px;
    border-radius: 34px;
    overflow: hidden;
    background: #102a43;
    border: 1px solid rgba(16, 42, 67, 0.08);
    box-shadow: 0 30px 80px rgba(16, 42, 67, 0.14);
  }

  .image-placeholder img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .image-placeholder::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 1;
    background:
      linear-gradient(180deg, rgba(7, 17, 31, 0.02) 0%, rgba(7, 17, 31, 0.72) 100%),
      linear-gradient(90deg, rgba(7, 17, 31, 0.28) 0%, rgba(7, 17, 31, 0.08) 100%);
  }

  .visual-content {
    position: absolute;
    left: 42px;
    right: 42px;
    bottom: 42px;
    z-index: 2;
    padding: 32px;
    border-radius: 28px;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.14);
    backdrop-filter: blur(16px);
  }

  .visual-content span {
    display: block;
    margin-bottom: 16px;
    color: #ddecea;
    font-size: 13px;
    font-weight: 900;
    letter-spacing: 0.16em;
  }

  .visual-content strong {
    display: block;
    color: #ffffff;
    font-size: clamp(28px, 4vw, 44px);
    line-height: 1;
    letter-spacing: -0.05em;
  }

  @media (max-width: 900px) {
    .intro-section {
      padding: 86px 0;
    }

    .intro-grid {
      grid-template-columns: 1fr;
      gap: 44px;
    }

    .image-placeholder {
      min-height: 380px;
    }
  }
</style>
